/** Points on a theme's Upgrade track, and on the loadout's. */
export const UPGRADE_TRACK_LENGTH = 3;

/** Boxes on a theme's Decay track. */
export const DECAY_TRACK_LENGTH = 3;

/** Circles on the Evolution track. */
export const EVOLUTION_POINTS_TRACK_LENGTH = 5;

/** Boxes on the Gain a Veteran Special Moment: it may be taken up to 3 times. */
export const VETERAN_SPECIALS_MOMENT_LENGTH = 3;

/** Themes a character starts with, loadout and crew theme excluded. */
export const STARTING_THEMES = 4;

/** Power tags a starting theme holds, the title tag included. */
export const STARTING_POWER_TAGS = 3;

/** Weakness tags a starting theme holds. */
export const STARTING_WEAKNESS_TAGS = 1;

/** The one starting theme the player may build larger takes these instead. */
export const LARGER_THEME_POWER_TAGS = 4;
export const LARGER_THEME_WEAKNESS_TAGS = 2;

/** Loadout Power a character starts with. */
export const STARTING_LOADOUT_POWER = 1;

/** Power a burnt tag adds. Theme specials raise it to 4 or 5 per burn. */
export const DEFAULT_BURN_VALUE = 3;

/** A status's tier limit, unless the player raises it for a rare edge case. */
export const DEFAULT_STATUS_LIMIT = 6;

/** Loadout Power a tag costs, and a wildcard tag. A flaw costs nothing. */
export const LOADOUT_TAG_COST = 1;
export const WILDCARD_TAG_COST = 2;
